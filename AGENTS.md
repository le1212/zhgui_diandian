# 项目协作规范

## 任务完成审查

- 每次任务完成后、最终交付前，必须使用 `$code-simplifier` 技能审查本次任务涉及的全部代码变更。
- 按该技能要求，在不改变功能的前提下简化和优化代码，并完成必要的验证；发现问题必须先修复，再向用户交付。
- 即使变更规模较小，也不得跳过此审查步骤。

## 方案标准

- 所有方案选择均以“企业级完美方案”为默认标准，综合考虑安全性、可靠性、可维护性、可扩展性、性能、可观测性、可测试性、兼容性与长期演进成本。
- 如多个目标存在冲突，应明确说明权衡，并选择整体质量最高、风险最低且适合生产环境长期维护的方案。
- 不采用仅能临时运行、明显积累技术债或缺少必要异常处理与边界保护的实现。

## 代码与 UI 规范

- 代码必须规范、美观、结构清晰、命名准确、职责单一，并遵循项目既有风格；避免重复代码、无意义抽象、过度嵌套和不必要的复杂度。
- UI 布局必须合理、美观且层级清楚，保持一致的间距、对齐、字体、颜色和交互反馈，并兼顾响应式、可访问性及不同内容状态。
- 新增或修改界面时，应完整处理加载、空状态、错误、禁用、溢出和不同屏幕尺寸等场景。
